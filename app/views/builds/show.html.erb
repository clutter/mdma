<p id="notice"><%= notice %></p>

<p>
  <strong>Upload and Push at:</strong>
  <%= @build.deployed_at %>
</p>

<p>
  <strong>Package</strong>
  <%= link_to "Download", rails_blob_url(@build.package, disposition: "attachment") %>
</p>

<%= link_to 'Edit', edit_build_path(@build) %> |
<%= link_to 'Back', builds_path %>
