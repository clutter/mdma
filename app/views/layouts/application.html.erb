<!DOCTYPE html>
<html lang="en">
<head>
  <title>Clutter - MDMA</title>
  <%= csrf_meta_tags %>
  <meta charset="utf-8">
  <meta content="width=device-width, initial-scale=1.0" name="viewport">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.3/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.4/css/all.css">
  <style>
    .navbar-light .navbar-nav .nav-link { color: #004085 }
    .navbar-light .navbar-nav .active .nav-link { color: #007bff }
  </style>
</head>
<body>
  <div class="container">
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
      <span class="navbar-brand">mdma</span>
      <% if logged_in? %>
      <div>
        <ul class="navbar-nav mr-auto">
          <%= nav_link_to 'Show builds', root_path, icon: 'list-alt' %>
          <%= nav_link_to 'Show devices', devices_path, icon: 'mobile' %>
          <%= nav_link_to 'Schedule new build', new_build_path, icon: 'cloud-upload-alt' %>
        </ul>
      </div>
      <% end %>
    </nav>
    <%= yield %>
  </div>
</body>
</html>